spring:
  application:
    name: sd-financial-instrument-valuation

server:
  port: 8080
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true   # /actuator/health/liveness + /readiness for K8s probes
  info:
    env:
      enabled: true

info:
  bian:
    serviceDomain: "Financial Instrument Valuation"
    businessArea: "Operations and Execution"
    businessDomain: "Markets and Treasury"
    functionalPattern: "Analyze"
    assetType: "Instrument Valuation"
    controlRecord: "Instrument Valuation Analysis"

resilience4j:
  circuitbreaker:
    instances:
      serviceDomain:
        slidingWindowSize: 20
        failureRateThreshold: 50
        waitDurationInOpenState: 10s

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
